[project]
name = "kokoro"
version = "0.1.0"
description = "Kokoro — Marketing Strategy Methodology"
requires-python = ">=3.9"

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]

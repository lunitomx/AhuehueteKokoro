# S40.9 Plan: Runtime Documentation and Share Readiness

## Story

- Epic: E40 Kokoro Operability Orchestrators
- Story: S40.9 Runtime Documentation and Share Readiness
- Branch: `codex/story-s40.9-runtime-share-readiness`

## Task 1: Create Share Readiness Command

Create:

- `.claude/commands/kokoro-share-readiness.md`

The command must include:

- Purpose and usage examples.
- Required knowledge files.
- Share readiness gates.
- Runtime truth table.
- Git and ignore checks.
- Secret/path scan.
- Pass/Hold/Private Only decision.
- Permission gate before publishing, packaging, or sharing private artifacts.

Verification:

```bash
rg -n "kokoro-privacy-protocol|kokoro-share-readiness|Pass|Hold|Private Only|GATE-NO-SENSITIVE-DATA|Permission Gate|Codex|Claude|Hermes" .claude/commands/kokoro-share-readiness.md
```

## Task 2: Update Routers

Update:

- `.claude/knowledge/kokoro-executive-router.md`
- `.claude/commands/kokoro.md`

Expected:

- `/kokoro-share-readiness` marked `Available`.
- Completed E40 routes no longer marked `planned` in main router examples.

Verification:

```bash
rg -n "Share Kokoro safely|/kokoro-share-readiness|Available|planned" .claude/knowledge/kokoro-executive-router.md .claude/commands/kokoro.md
```

## Task 3: Update Runtime Docs

Update:

- `README.md`
- `AGENTS.md`

Expected:

- README stops claiming Codex auto-loads `.claude/commands/` as native slash commands.
- README explains Claude, Codex, and Hermes differences.
- README lists operating runs and a no-MCP demo path.
- AGENTS includes the E40 operating run map for Codex.

Verification:

```bash
rg -n "Codex.*does not|manual|kokoro-google-ads-run|kokoro-weekly-marketing-run|kokoro-creative-campaign-run|kokoro-launch-run|kokoro-acquisition-run|kokoro-share-readiness|no MCP" README.md AGENTS.md
```

## Task 4: Share Safety Verification

Run:

```bash
git check-ignore -v .kokoro/clients.json reports/cliente_01/pulse.md generated/cliente_01/ad.png exports/cliente_01/export.csv
rg -n --hidden --glob '!/.git/**' --glob '!/.agents/**' --glob '!work/research/**' "(sk-[A-Za-z0-9_-]{20,}|AIza[0-9A-Za-z_-]{20,}|EAA[A-Za-z0-9]{20,}|access[_-]?token|refresh[_-]?token|client[_-]?secret)" .
```

Expected:

- Ignored private paths are protected.
- Secret scan has no real credentials.
- Any hits are documentation-only placeholders or regex instructions.

## Task 5: Retrospective and Epic Progress

Capture:

- `work/epics/e40-kokoro-operability-orchestrators/stories/s40.9-progress.md`
- `work/epics/e40-kokoro-operability-orchestrators/stories/s40.9-retrospective.md`

Then update E40 scope progress after merge.

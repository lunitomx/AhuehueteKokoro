# S41.1 Design: Contract Discovery for Negative Keywords

## Complexity

Simple to moderate (single integration slice).

## Problem

No hay certeza sobre dónde vive exactamente el conector que opera keywords negativas en este checkout público, ni si el contrato soporta `delete`/estado. Diseñar `S41.2` sin ese inventario crea riesgo de romper `add`.

## Value

Con contrato y restricciones claros, la siguiente historia puede implementar eliminación sin ruido de compatibilidad y con respuesta consistente.

## Approach

1. Localizar el surface de conector real.
2. Extraer schema vigente de operación para negativas.
3. Registrar diferencias entre intención funcional (`delete`) y capacidades actuales.
4. Proponer ruta de implementación compatible:
   - extender acción existente, o
   - introducir acción nueva con bandera de compatibilidad si no hay contrato de estado.

### Resultados de descubrimiento

- No se identificó código de conector en este checkout.
- La siguiente decisión está restringida a la evidencia actual del issue y de S41:
  - `contract_has_add_only`: `unknown` (pendiente de confirmar desde fuente real).
  - `delete_tool_exists`: `unknown` (asumir `false` hasta confirmación).
  - `state_field`: `unknown`.
- Recomendación para `S41.2`: el contrato objetivo debe versionarse contra la fuente real de integración antes de cualquier implementación en este repositorio.

## Components Affected

| Component | Change Type | Why it matters |
|---|---|---|
| Google Ads conector | discover-only | Determinar fuente real de truth para acciones |
| `.raise/artifacts/s41.1-design.yaml` | create | Guardar diseño para downstream CI/seguimiento |
| `work/epics/e41.../scope.md` | modify | Verificar dependencies |
| `work/epics/e41.../stories/s41.1-*` | create | Capturar evidencia trazable |

## Examples

### Input schema example (actual)

```json
{
  "action": "add_negative_keywords",
  "ad_group_id": "string",
  "keywords": [
    {"text": "free trial", "match_type": "BROAD"}
  ]
}
```

### Expected contract delta note (target)

```text
If delete exists:
- action: remove_negative_keywords
Else:
- action: add_negative_keywords + op="delete" + state tracking adapter
```

## Acceptance Criteria

### MUST

- Ubicar path del handler de negativas y adjuntarlo a esta historia.
- Definir si existe estado de keyword (`active`/`removed`) o solo soft-delete por operación externa.
- Definir si se requiere `dry_run` para borrado.
- Detectar posibles diferencias entre SDK/API y runtime disponible en MCP local.

### SHOULD

- Incluir evidencia de naming: `add_keywords`, `add_negative_keywords`, `negative_keyword` etc.
- Listar un plan de compatibilidad para clientes que consumen `add` hoy.

### MUST NOT

- Proponer implementación antes de confirmar esquema actual.
- Asumir soportes de `match_type` no existentes.

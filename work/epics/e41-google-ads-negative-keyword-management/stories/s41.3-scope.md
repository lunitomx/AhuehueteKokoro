# S41.3 Scope: Implement Local Delete Adapter

## Objective

Implementar un adapter local de Kokoro para `negative_keyword.delete` usando la surface oficial del MCP solo para lookup/discovery.

## In Scope

- Exponer acción local `negative_keyword.delete`.
- Resolver contexto con `search` y `metadata` del MCP oficial.
- Mantener compatibilidad con `add`.
- Registrar auditoría y estados parciales.

## Out of Scope

- Cambiar el servidor oficial `googleads/google-ads-mcp`.
- Agregar mutate tools al provider externo.
- UI o workflows fuera del router/tactical command layer.

## Dependencies

- S41.1 discovery closed.
- S41.2 contract closed.
- Runtime local path disponible.

## Done Criteria

- [ ] Adapter local implementado o ruta de implementación confirmada.
- [ ] Mutation path de negativas cubierto por tests.
- [ ] No regressions in `add`.
- [ ] Audit parity and error handling documented.

## Risk

- Si no existe runtime local disponible, la historia permanece bloqueada y se replanea en el siguiente sprint.
